version: 1
backend:
  phases:
    build:
      commands:
        - echo "Backend ya configurado con Amplify CLI"
frontend:
  phases:
    preBuild:
      commands:
        - echo "🔍 === DEBUG - YA ESTAMOS EN WEBSITE/ ==="
        - pwd
        - ls -la
        - echo "🔍 Verificando package.json:"
        - ls -la package.json || echo "❌ package.json no encontrado"
        - echo "🔍 Verificando Node/npm:"
        - node --version
        - npm --version
        - echo "🔍 Limpiando cache npm:"
        - npm cache clean --force
        - echo "🔍 === INSTALL DIRECTO (SIN CD) ==="
        - echo "📦 Instalando dependencias:"
        - npm install --legacy-peer-deps --no-audit --no-fund
    build:
      commands:
        - echo "🚀 === BUILD DIRECTO (SIN CD) ==="
        - npm run build
        - echo "✅ Verificando build output:"
        - ls -la .next/ || echo "❌ .next/ no encontrado"
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
env:
  variables:
    NEXT_PUBLIC_APPSYNC_API_URL: $NEXT_PUBLIC_APPSYNC_API_URL
    NEXT_PUBLIC_APPSYNC_REGION: $NEXT_PUBLIC_APPSYNC_REGION
    NEXT_PUBLIC_APPSYNC_API_KEY: $NEXT_PUBLIC_APPSYNC_API_KEY
