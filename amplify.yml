version: 1
backend:
  phases:
    build:
      commands:
        - echo "Backend ya configurado con Amplify CLI"
frontend:
  phases:
    preBuild:
      commands:
        - echo "🚀 === INSTALL Y BUILD EN PREBUILD ==="
        - pwd
        - ls -la
        - echo "🔍 Entrando a website y ejecutando todo:"
        - cd website && npm install --legacy-peer-deps --no-audit --no-fund && npm run build && ls -la .next/
    build:
      commands:
        - echo "✅ === BUILD COMPLETADO EN PREBUILD ==="
        - echo "📁 Verificando artifacts desde raíz:"
        - ls -la website/.next/ || echo "⚠️ Checking artifacts..."
  artifacts:
    baseDirectory: website/.next
    files:
      - '**/*'
  cache:
    paths:
      - website/node_modules/**/*
env:
  variables:
    NEXT_PUBLIC_APPSYNC_API_URL: $NEXT_PUBLIC_APPSYNC_API_URL
    NEXT_PUBLIC_APPSYNC_REGION: $NEXT_PUBLIC_APPSYNC_REGION
    NEXT_PUBLIC_APPSYNC_API_KEY: $NEXT_PUBLIC_APPSYNC_API_KEY
