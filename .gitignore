# Gitignore global para el proyecto Coding Challenge

# Archivos de sistema macOS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Logs y temporales generales
*.log
logs/
*.tmp
*.temp


# --- AWS Amplify (Generados y Locales - NO SUBIR) ---
# Ignora toda la carpeta 'amplify/' por defecto
amplify/

# Excepciones: Archivos de configuración de Amplify que SÍ deben versionarse
# Son necesarios para compartir la configuración del proyecto entre desarrolladores
# y para que Amplify Console sepa cómo desplegar el backend.
!amplify/.config/local-aws-info.json     # Información del perfil AWS local
!amplify/.config/local-env-info.json     # Información del entorno local (nombre del entorno, etc.)
!amplify/backend/backend-config.json     # La definición de tus recursos de backend (API, Auth, Storage)
!amplify/team-provider-info.json         # Mapeo de entornos a cuentas AWS

# Otros archivos generados por Amplify que NUNCA deben subirse
# Aunque 'amplify/' ya los ignora, estas reglas son más específicas por seguridad.
amplify/#current-cloud-backend/
amplify/backend/.temp/
amplify/logs/
amplify/mock-data/
amplify/mock-api-resources/
amplify/backend/amplify-meta.json        # Archivo de metadatos de despliegue
amplify-build-config.json
amplifyconfiguration.json
amplifyconfiguration.dart
amplify-gradle-config.json
amplifytools.xcconfig
.secret-*

# AWS CLI credenciales y configs
.aws/
credentials
config

# Config de frontend generado (contiene info sensible)
**/aws-exports.js

# Next.js build output
.next/
out/


# Optional npm cache directory
.npm

# Dependencies
node_modules/
*/node_modules/

# Dependency directories
jspm_packages/

# Optional eslint cache
.eslintcache

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
venv/
env/
ENV/
env.bak/
venv.bak/
